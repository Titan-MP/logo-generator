const svgGen = require('./svgGen');
const questions = require('./questions');
const { Square, Circle, Triangle } = require('./shape');

module.exports = { questions, svgGen, Square, Circle, Triangle };